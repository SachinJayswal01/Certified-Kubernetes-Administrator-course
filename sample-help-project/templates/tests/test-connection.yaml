apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sample-help-project.fullname" . }}-test-connection"
  labels:
    {{- include "sample-help-project.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sample-help-project.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
