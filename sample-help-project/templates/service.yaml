apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-help-project.fullname" . }}
  labels:
    {{- include "sample-help-project.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sample-help-project.selectorLabels" . | nindent 4 }}
